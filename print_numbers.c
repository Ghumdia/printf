#include "main.h"

/**
 * print_i - prints integer
 * @i: integer
 *
 * Return: number of chars printed
 */

int print_i(va_list i)
{
	int a[10];
	int j, m, n, sum, count;

	n = va_arg(i, int);
	coun
}
